# VPS Provisioner Configuration

# REQUIRED: Change this in production!
API_KEY=CHANGE-ME-TO-SOMETHING-SECURE-XkP9mL2vQ8

# Optional: Database path (default: jobs.db in current directory)
# DATABASE_PATH=/var/lib/vps-provisioner/jobs.db

# Optional: Maximum age of completed jobs in hours (default: 24)
# MAX_JOB_AGE_HOURS=24

# Optional: Job cleanup interval in seconds (default: 3600 = 1 hour)
# JOB_CLEANUP_INTERVAL=3600

# Optional: SSH connection timeout in seconds (default: 30)
# SSH_TIMEOUT=30

# Optional: Maximum SSH connection retries (default: 15)
# SSH_MAX_RETRIES=15

# Optional: Installation timeout in seconds (default: 900 = 15 minutes)
# INSTALL_TIMEOUT=900

# Optional: Log directory (default: /tmp/vps-provisioner)
# LOG_DIR=/var/log/vps-provisioner
