# VPS Provisioner v2.0 - Quick Start

## ✅ Что исправлено

### Критические улучшения:
1. ✅ **SQLite вместо in-memory** - джобы не теряются при перезапуске
2. ✅ **API Key auth** - защита от несанкционированного доступа
3. ✅ **Валидация входных данных** - проверка IP, паролей, app names
4. ✅ **Idempotent скрипты** - можно перезапускать при ошибках
5. ✅ **Retry logic с exponential backoff** - умное ожидание SSH
6. ✅ **Production-ready** - Gunicorn + systemd service
7. ✅ **Автоочистка старых джобов** - не забивается база
8. ✅ **Логи установки** - всё сохраняется в /tmp
9. ✅ **Улучшенная обработка ошибок** - детальные сообщения
10. ✅ **SSH переиспользование** - одно соединение на всю установку

### Новые приложения:
- ✅ **Outline** - Notion alternative (лёгкий, красивый)
- ✅ **Vaultwarden** - Password manager
- ✅ **3X-UI** - Продвинутый VPN (VMess, VLESS, Trojan) - лучше для России/Китая
- ✅ **FileBrowser** - Лёгкий файловый менеджер

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонируй файлы в /opt/vps-provisioner
cd /opt/vps-provisioner

# Установи зависимости
pip install -r requirements.txt

# Настрой .env
cp .env.example .env
nano .env  # Поставь свой API_KEY
```

### 2. Запуск (development)

```bash
python app.py
```

### 3. Запуск (production)

```bash
# Установи как systemd service
cp vps-provisioner.service /etc/systemd/system/
nano /etc/systemd/system/vps-provisioner.service  # Поставь свой API_KEY

systemctl daemon-reload
systemctl enable vps-provisioner
systemctl start vps-provisioner

# Проверь статус
systemctl status vps-provisioner

# Логи
journalctl -u vps-provisioner -f
```

## 📡 Использование API

### Простой пример (curl)

```bash
# Health check
curl http://localhost:5001/health

# Запуск установки n8n
curl -X POST http://localhost:5001/provision \
  -H "X-API-Key: твой-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "ip_address": "95.179.200.45",
    "username": "root",
    "password": "твой-пароль-сервера",
    "app": "n8n"
  }'

# Проверка статуса
curl http://localhost:5001/status/job-id-который-вернули
```

### Python пример

```python
import requests
import time

API_URL = "http://localhost:5001"
API_KEY = "твой-api-key"

# Запускаем установку
response = requests.post(
    f"{API_URL}/provision",
    headers={"X-API-Key": API_KEY},
    json={
        "ip_address": "95.179.200.45",
        "username": "root",
        "password": "пароль",
        "app": "n8n"
    }
)

job_id = response.json()["job_id"]

# Ждём завершения
while True:
    status = requests.get(f"{API_URL}/status/{job_id}").json()
    print(f"{status['progress']}% - {status['message']}")
    
    if status['status'] in ['completed', 'failed']:
        break
    
    time.sleep(10)

# Получаем результат
if status['status'] == 'completed':
    result = status['result']
    print(f"URL: {result['url']}")
    print(f"Login: {result['credentials']['username']}")
    print(f"Password: {result['credentials']['password']}")
```

### Использование тест-скрипта

```bash
# Автоматический тест
python test_api.py 95.179.200.45 root password123 n8n

# Он сам:
# 1. Проверит health
# 2. Запустит установку
# 3. Будет показывать прогресс
# 4. Выведет результат с URL и кредами
```

## 📦 Поддерживаемые приложения

| App | Описание | RAM | Порт | Время установки |
|-----|----------|-----|------|----------------|
| **n8n** | Автоматизация (Zapier) | 300MB | 5678 | 5-10 мин |
| **wireguard** | VPN с UI | 50MB | 5000 + 51820/udp | 5-10 мин |
| **outline** | Wiki/Notion | 500MB | 3000 | 10-15 мин |
| **vaultwarden** | Password manager | 30MB | 8080 | 3-5 мин |
| **3x-ui** | VPN панель (VMess/VLESS) | 100MB | 54321 | 5-10 мин |
| **seafile** ⭐ | Облако с мобильными приложениями | 500MB | 8000 | 10-15 мин |
| **filebrowser** | Файловый менеджер | 30MB | 8081 | 3-5 мин |

## 🎯 Рекомендации по серверам

### Твой сервер (AMD Epyc 3 vCore / 8GB RAM / 150GB) идеален для:

✅ **LITE tier (1 клиент):**
- WireGuard ✅
- n8n ✅
- FileBrowser ✅
- Vaultwarden ✅
- **Итого:** ~500MB RAM, остаётся 7.5GB запаса

✅ **Или:**
- Outline + FileBrowser + 3X-UI
- **Итого:** ~700MB RAM

❌ **НЕ ставь:**
- Nextcloud (жрёт 2GB+)
- GitLab (жрёт 4GB+)
- Несколько тяжёлых приложений одновременно

## 🔐 Безопасность

### API Key

**ОБЯЗАТЕЛЬНО** смени API ключ:

```bash
# В .env
API_KEY=генерируй-длинный-случайный-ключ

# Или при запуске
export API_KEY="твой-супер-секретный-ключ"
gunicorn ...
```

### Не открывай наружу!

Provisioner НЕ должен быть доступен из интернета. Используй:

1. **VPN/Tailscale** для доступа
2. **Nginx с auth_basic** если нужен HTTP
3. **Firewall** чтобы разрешить только с твоего IP

```bash
# Пример: разрешить только с твоего IP
ufw allow from ТВО_IP to any port 5001
```

## 🐛 Troubleshooting

### Логи установки

```bash
# Все установки сохраняются в /tmp
ls -la /tmp/*_install_*.log

# Смотри последнюю установку
tail -100 /tmp/n8n_install_95_179_200_45.log
```

### База данных джобов

```bash
# Посмотри джобы
sqlite3 jobs.db "SELECT job_id, status, app, created_at FROM jobs ORDER BY created_at DESC LIMIT 10;"

# Очисти старые
curl -X POST http://localhost:5001/cleanup -H "X-API-Key: твой-ключ"
```

### SSH не подключается

Проблема: `SSH timeout after 15 attempts`

Решения:
1. Подожди 5-10 минут после создания VPS
2. Проверь креды: `ssh root@IP`
3. Проверь firewall на сервере
4. Попробуй другой порт SSH (если не 22)

### Docker не устанавливается

Проблема: `Docker installation failed`

Решения:
1. Старое ядро Linux → обнови OS
2. Мало места → проверь `df -h`
3. Конфликт с containerd → `apt remove containerd`

### Контейнер не запускается

```bash
# Зайди на сервер и смотри логи
ssh root@IP
docker ps -a
docker logs <container_name>

# Проверь порты
netstat -tulpn | grep <port>

# Рестарт контейнера
docker restart <container_name>
```

## 📊 Мониторинг

### Статистика джобов

```bash
curl http://localhost:5001/stats -H "X-API-Key: твой-ключ"
```

Вернёт:
```json
{
  "total": 42,
  "by_status": {
    "completed": 35,
    "failed": 5,
    "installing": 2
  },
  "by_app": {
    "n8n": 20,
    "wireguard": 15,
    "outline": 7
  }
}
```

### Логи сервиса

```bash
# Real-time логи
journalctl -u vps-provisioner -f

# Последние 100 строк
journalctl -u vps-provisioner -n 100

# Логи за сегодня
journalctl -u vps-provisioner --since today
```

## 🚀 Workflow для твоего бизнеса

### Когда клиент покупает LITE tier:

```python
# 1. Заказываешь VPS у SpaceCore (вручную или через API)
# Получаешь: IP, root password

# 2. Ждёшь 5 минут пока сервер поднимется

# 3. Вызываешь provisioner API для каждого приложения:
apps = ['wireguard', 'n8n', 'vaultwarden', 'filebrowser']

for app in apps:
    requests.post(
        "http://localhost:5001/provision",
        headers={"X-API-Key": API_KEY},
        json={
            "ip_address": customer_ip,
            "username": "root",
            "password": server_password,
            "app": app
        }
    )

# 4. Ждёшь когда все джобы завершатся (~20-30 минут на 4 аппа)

# 5. Собираешь результаты и отправляешь клиенту email:
"""
Привет! Твой VPS готов:

WireGuard VPN: http://IP:5000
Username: admin
Password: xxx

n8n Automation: http://IP:5678
Username: admin
Password: yyy

...
"""
```

## 📈 Следующие шаги

### Улучшения которые можно добавить:

1. **Email уведомления** - шли письмо когда установка готова
2. **Webhook support** - вызывай webhook когда джоб завершён
3. **Web dashboard** - красивый UI вместо API
4. **Batch provisioning** - ставь сразу несколько аппов одним запросом
5. **Custom domains с SSL** - автоматический Let's Encrypt
6. **Backup/restore** - автобэкапы для каждого аппа
7. **Monitoring** - Uptime Kuma для всех серверов
8. **PostgreSQL вместо SQLite** - для более чем 100 серверов

### Добавление новых приложений:

1. Создай `provisioners/seafile.py` (скопируй структуру из outline.py)
2. Создай `templates/seafile_install.sh`
3. Добавь в `app.py`: `'seafile': setup_seafile`
4. Добавь в `config.py`: `SUPPORTED_APPS`
5. Тестируй!

## ⚡ Производительность

С 4 воркерами Gunicorn можешь обрабатывать:
- **~4 одновременных установок** (по одной на воркер)
- **Сотни запросов status/health** в секунду
- **Тысячи джобов в базе** без проблем

Если нужно больше - просто увеличь воркеры:
```bash
gunicorn -w 8 ...  # 8 воркеров = 8 параллельных установок
```

## 📞 Поддержка

Если что-то сломалось:

1. **Посмотри логи**: `journalctl -u vps-provisioner -f`
2. **Посмотри логи установки**: `/tmp/*_install_*.log`
3. **Проверь статус джоба**: `curl /status/job-id`
4. **Проверь базу**: `sqlite3 jobs.db "SELECT * FROM jobs WHERE status='failed'"`

---

**Готово к запуску!** 🚀

Начни с тестовой установки на одном сервере:
```bash
python test_api.py 95.179.200.45 root password n8n
```

Если всё ок - разворачивай в продакшн и зарабатывай! 💰
